name: Keep Render Alive
on:
  schedule:
    - cron: '*/15 * * * *'
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Wake up API
        run: curl -sf https://survivor-ligamx-bot.onrender.com/health || echo "⏳ API despertando..."
