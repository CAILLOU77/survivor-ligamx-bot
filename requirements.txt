# Dependencias del bot Survivor Liga MX (versiones fijadas para builds reproducibles).
# Probadas con Python 3.9. Actualiza con cuidado y corre `pytest tests/` antes de subir.

# Web / API
fastapi==0.128.8
uvicorn[standard]==0.39.0
slowapi==0.1.10

# Datos / modelo
numpy==2.0.2
scipy==1.13.1
pandas==2.3.3
pyarrow==21.0.0
requests==2.32.5
feedparser==6.0.12

# Base de datos (Postgres en prod; SQLite en local no necesita extra)
psycopg2-binary==2.9.12

# Utilidades
python-dotenv==1.2.1
